<Page
    x:Class="YtDlpGUI.Views.HistoryPage"
    x:Name="RootPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <Grid Padding="20,8,20,20" RowSpacing="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Spacing="8">
            <Button Command="{Binding RefreshCommand}" Content="Refresh" Style="{StaticResource PrimaryActionButtonStyle}" />
            <Button Command="{Binding ClearHistoryCommand}" Content="Clear History" />
            <TextBlock Margin="12,0,0,0" VerticalAlignment="Center" Text="{Binding StatusMessage}" />
        </StackPanel>

        <Border Grid.Row="1" Style="{StaticResource CardBorderStyle}">
            <ListView
                ItemsSource="{Binding Entries}"
                SelectedItem="{Binding SelectedEntry, Mode=TwoWay}">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Border Margin="0,0,0,8" Padding="10" CornerRadius="8" BorderThickness="1" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}">
                            <Grid RowSpacing="6">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                </Grid.RowDefinitions>

                                <Grid ColumnSpacing="8">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>
                                    <StackPanel Grid.Column="0">
                                        <TextBlock FontWeight="SemiBold" Text="{Binding Title}" />
                                        <TextBlock FontSize="12" Opacity="0.7" Text="{Binding Url}" TextTrimming="CharacterEllipsis" />
                                    </StackPanel>
                                    <TextBlock Grid.Column="1" Text="{Binding CompletedAtText}" />
                                </Grid>

                                <StackPanel Grid.Row="1">
                                    <TextBlock Text="{Binding OutputPath}" TextTrimming="CharacterEllipsis" />
                                    <TextBlock Foreground="{ThemeResource SystemFillColorCriticalBrush}" Text="{Binding ErrorMessage}" TextTrimming="CharacterEllipsis" />
                                </StackPanel>

                                <StackPanel Grid.Row="2" Orientation="Horizontal" Spacing="8">
                                    <Button Command="{Binding DataContext.RequeueCommand, ElementName=RootPage}" CommandParameter="{Binding}" Content="Requeue" />
                                    <Button Command="{Binding DataContext.OpenOutputCommand, ElementName=RootPage}" CommandParameter="{Binding}" Content="Open" />
                                    <Button Command="{Binding DataContext.OpenFolderCommand, ElementName=RootPage}" CommandParameter="{Binding}" Content="Open Folder" />
                                    <Button Command="{Binding DataContext.CopyCommandPreviewCommand, ElementName=RootPage}" CommandParameter="{Binding}" Content="Copy Command" />
                                </StackPanel>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Border>
    </Grid>
</Page>

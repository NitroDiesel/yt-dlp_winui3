<Page
    x:Class="YtDlpGUI.Views.QuickDownloadPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <ScrollViewer>
        <StackPanel Padding="20,8,20,20" Spacing="16">
            <Border Style="{StaticResource CardBorderStyle}">
                <StackPanel Spacing="12">
                    <TextBlock Style="{StaticResource SectionTitleStyle}" Text="Quick Download" />

                    <TextBlock Text="Video or playlist URL" />
                    <TextBox
                        PlaceholderText="Paste URL here..."
                        Text="{Binding Url, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />

                    <Grid ColumnSpacing="12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0" Spacing="4">
                            <TextBlock Text="Mode" />
                            <ComboBox
                                ItemsSource="{Binding DownloadModes}"
                                SelectedItem="{Binding SelectedMode, Mode=TwoWay}">
                                <ComboBox.ItemTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding Converter={StaticResource EnumDisplayNameConverter}}" />
                                    </DataTemplate>
                                </ComboBox.ItemTemplate>
                            </ComboBox>
                        </StackPanel>

                        <StackPanel Grid.Column="1" Spacing="4">
                            <TextBlock Text="{Binding QualityLabel}" />
                            <ComboBox
                                ItemsSource="{Binding QualityOptions}"
                                SelectedItem="{Binding SelectedQualityOption, Mode=TwoWay}" />
                        </StackPanel>

                        <StackPanel Grid.Column="2" Spacing="4">
                            <TextBlock Text="{Binding FormatLabel}" />
                            <ComboBox
                                ItemsSource="{Binding FormatOptions}"
                                SelectedItem="{Binding AudioFormat, Mode=TwoWay}" />
                        </StackPanel>
                    </Grid>

                    <Border
                        Background="{ThemeResource SubtleFillColorSecondaryBrush}"
                        BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                        BorderThickness="1"
                        CornerRadius="10"
                        Padding="12"
                        Visibility="{Binding CustomQualityVisibility}">
                        <StackPanel Spacing="8">
                            <TextBlock FontWeight="SemiBold" Text="Custom video quality" />
                            <TextBlock
                                Opacity="0.75"
                                Text="Use these only when Quality is set to Custom. Format selector overrides yt-dlp's default selection, while format sort biases the best match."
                                TextWrapping="Wrap" />
                            <TextBox
                                Header="Custom format selector (-f)"
                                PlaceholderText="Example: bv*[height<=1080]+ba/b"
                                Text="{Binding CustomFormat, Mode=TwoWay}" />
                            <TextBox
                                Header="Format sort (-S)"
                                PlaceholderText="Example: res:1080,fps,hdr:12"
                                Text="{Binding FormatSort, Mode=TwoWay}" />
                        </StackPanel>
                    </Border>

                    <TextBlock Text="Output folder" />
                    <Grid ColumnSpacing="8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <TextBox Grid.Column="0" Text="{Binding OutputDirectory, Mode=TwoWay}" />
                        <Button
                            Grid.Column="1"
                            Command="{Binding BrowseOutputFolderCommand}"
                            Content="Browse"
                            MinWidth="90" />
                    </Grid>

                    <TextBlock Text="Filename template" />
                    <TextBox Text="{Binding OutputTemplate, Mode=TwoWay}" />

                    <Expander Header="More options" IsExpanded="False">
                        <StackPanel Margin="0,8,0,0" Spacing="8">
                            <ToggleSwitch
                                Header="Download subtitles"
                                IsOn="{Binding DownloadSubtitles, Mode=TwoWay}" />
                            <ToggleSwitch
                                Header="Download auto subtitles"
                                IsOn="{Binding DownloadAutoSubtitles, Mode=TwoWay}" />
                            <ToggleSwitch
                                Header="Embed subtitles"
                                IsOn="{Binding EmbedSubtitles, Mode=TwoWay}" />
                            <ToggleSwitch
                                Header="Embed metadata"
                                IsOn="{Binding EmbedMetadata, Mode=TwoWay}" />
                            <ToggleSwitch
                                Header="Embed thumbnail"
                                IsOn="{Binding EmbedThumbnail, Mode=TwoWay}" />

                            <StackPanel Spacing="4">
                                <TextBlock Text="Playlist handling" />
                            <ComboBox
                                ItemsSource="{Binding PlaylistModes}"
                                SelectedItem="{Binding SelectedPlaylistMode, Mode=TwoWay}">
                                <ComboBox.ItemTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding Converter={StaticResource EnumDisplayNameConverter}}" />
                                    </DataTemplate>
                                </ComboBox.ItemTemplate>
                            </ComboBox>
                        </StackPanel>
                        </StackPanel>
                    </Expander>

                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <Button
                            Click="OnAddDownloadClick"
                            Content="Add Download..."
                            MinWidth="130" />
                        <Button
                            Command="{Binding DownloadNowCommand}"
                            Content="Download Now"
                            Style="{StaticResource PrimaryActionButtonStyle}" />
                        <Button
                            Command="{Binding AddToQueueCommand}"
                            Content="Add To Queue"
                            MinWidth="120" />
                        <Button
                            Command="{Binding CancelCurrentCommand}"
                            Content="Cancel Current"
                            MinWidth="120" />
                    </StackPanel>

                    <TextBlock Foreground="{ThemeResource SystemFillColorCriticalBrush}" Text="{Binding StatusMessage}" TextWrapping="Wrap" />
                </StackPanel>
            </Border>

            <Border Style="{StaticResource CardBorderStyle}">
                <StackPanel Spacing="10">
                    <TextBlock Style="{StaticResource SectionTitleStyle}" Text="Live Progress" />

                    <TextBlock FontWeight="SemiBold" Text="{Binding CurrentTitle}" TextWrapping="Wrap" />
                    <Grid ColumnSpacing="8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <ProgressBar Grid.Column="0" Maximum="100" Value="{Binding CurrentProgress}" />
                        <TextBlock Grid.Column="1" VerticalAlignment="Center" Text="{Binding CurrentProgressText}" />
                    </Grid>

                    <Grid ColumnSpacing="12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0">
                            <Run Text="Status: " />
                            <Run Text="{Binding CurrentStatus}" />
                        </TextBlock>
                        <TextBlock Grid.Column="1">
                            <Run Text="Speed: " />
                            <Run Text="{Binding CurrentSpeed}" />
                        </TextBlock>
                        <TextBlock Grid.Column="2">
                            <Run Text="ETA: " />
                            <Run Text="{Binding CurrentEta}" />
                        </TextBlock>
                    </Grid>

                    <TextBlock FontWeight="SemiBold" Text="Output log" />
                    <Border Height="220" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="8" Padding="8">
                        <ListView ItemsSource="{Binding LiveLogs}">
                            <ListView.ItemTemplate>
                                <DataTemplate>
                                    <TextBlock TextWrapping="WrapWholeWords">
                                        <Run Text="{Binding Severity}" />
                                        <Run Text=": " />
                                        <Run Text="{Binding Message}" />
                                    </TextBlock>
                                </DataTemplate>
                            </ListView.ItemTemplate>
                        </ListView>
                    </Border>
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</Page>
